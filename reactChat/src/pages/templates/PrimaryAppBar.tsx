import { AppBar, Toolbar } from "@mui/material";

const PrimaryAppBar = () => {
  return (
    <AppBar>
      <Toolbar variant="dense">8</Toolbar>
    </AppBar>
  );
};

export default PrimaryAppBar;
